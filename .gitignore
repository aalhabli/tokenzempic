# local planning docs
INITIAL_RESEARCH.md
EXECUTION_PLAN.md
learn/

# dependencies and build output
node_modules/
dist/
*.tsbuildinfo
oclif.manifest.json

# agent metadata that `sf agent publish` generates from the authoring bundle.
# It holds org-specific IDs, and the bundle is the source of truth.
demo/force-app/main/default/bots/
demo/force-app/main/default/genAiPlannerBundles/

# local data and secrets
*.db
.tokenzempic/
.env
.sf/
.sfdx/
