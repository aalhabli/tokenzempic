# Contributing

Glad you're here. A few things before you open a PR:

- Open an issue first for anything bigger than a small fix, so we don't waste your time.
- `npm install && npm run build && npm test` should pass before and after your change.
- New logic needs a test. Codegen changes need a parity test showing generated output against a recorded session.

That's it.
