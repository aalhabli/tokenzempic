{
  "name": "@tokenzempic/core",
  "version": "0.0.1",
  "description": "Ingestion, clustering, and codegen engine. No CLI, no UI, no network beyond the Salesforce and LLM clients handed to it.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
